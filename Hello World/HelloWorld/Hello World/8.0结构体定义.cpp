#include <iostream>
using namespace std;



/*int main() {

	//创建结构体时struct不可以省略，定义结构体时可以省略
	struct student
	{
		string name;
		int age;
		int core;
	}s3;//创建结构体时顺便把变量创建出来

	//使用结构体 创建变量 三种方式：
	//1.直接给s1的属性赋值，访问用.     访问结构体变量中的属性
	//结构体名字  变量名字
	 student s1;//定义结构体时struct可以省略
	s1.name = "张三";
	s1.age = 18;
	s1.core = 70;
	cout << "姓名 " << s1.name << " 年龄 " << s1.age << " 分数 " << s1.core << endl;

	//结构体 变量名 = {值....}
	struct student s2 = {"张三",18,70 };
	cout << "姓名 " << s2.name << " 年龄 " << s2.age << " 分数 " << s2.core << endl;

	//创建结构体时顺便把变量创建出来
	s3.name = "王五";
	s3.age = 19;
	s3.core = 85;
	cout << "姓名 " << s3.name << " 年龄 " << s3.age << " 分数 " << s3.core << endl;

	system("pause");
	return 0;
}*/