#include <iostream>
using namespace std;

/*int main() {

	int a = 10;
	int b = 20;
	//1.const 修饰指针
	const int* p = &a;//常量指针
	//*p = 20;  指针指向的值不可以修改
	p = &b;

	//2.const修饰常量
	int* const p1 = &a; //指针常量
	*p1 = 30;
	//p1 = &b;指针的指向不可以修改，指针指向的值可以修改

	//3.const 修饰指针和常量
	const int* const p2 = &a;
	//*p2 = 50;
	//p2 = &b;
	//指针的指向和指针的值都不可以修改。

	system("pause");
	return 0;
}*/